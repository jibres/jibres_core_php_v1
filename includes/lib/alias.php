<?php
namespace lib;

class alias
{

	public static function url()
	{
		// $subdomain = \dash\url::subdomain();
		// $subdomain = \dash\safe::safe($subdomain);

		// if(!$subdomain)
		// {
		// 	return false;
		// }

		// \lib\store::init($subdomain);

		// if(!\lib\store::id())
		// {
		// 	return false;
		// }

		// if(\lib\store::detail('plan') !== 'standard')
		// {
		// 	return false;
		// }

		// if(!\lib\store::detail('alias') === \dash\url::base())
		// {
		// 	return true;
		// }

		return false;
	}
}
?>